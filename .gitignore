# Large data files (exceed GitHub 50MB recommendation)
clients/bwh/directorystructure.txt

# Node
node_modules/
dist/

# Secrets
keys/

# OS
.DS_Store
Thumbs.db

# Runtime state (changes every iteration, not engine code)
.gsd/supervisor/agent-cooldowns.json
.gsd/agent-handoff/
.claude/scheduled_tasks.lock
bridge.log

# Temp/debug files (prevent root clutter)
test-*.ps1
fix-*.py
check-*.ps1
kill-*.ps1
restart-*.ps1
*-err.log
*-out.log
v3-pipeline.log
v3-restart-*.log

# Project artifacts (generated by agents into wrong dir)
db/
# NOTE: src/ was previously ignored here but V4.1 TypeScript harness lives in src/
# If external projects generate src/ here, clean them manually
!src/

# Graphify knowledge graph output (regenerated per-machine)
graphify-out/

# External project repos (managed by GSD, not tracked here)
external/
